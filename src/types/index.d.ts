export type Author = { name: string; handle_name: string; description: string };
export type skill = { name: string; rating: number; description?: string };
export type ReleaseHistory = { date: Date; ver?: string; note: string };
